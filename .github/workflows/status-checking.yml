name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  simple_status_checking_pipeline:
    runs-on: ubuntu-latest
    # Limit permissions for security best practices
    permissions:
      contents: read
      pull-requests: read
      checks: write
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ${{ secrets.POSTGRES_USER_TEST || 'postgres' }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST || 'postgres' }}
          POSTGRES_DB: finance_tracker_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      # Database configuration (shared across steps)
      POSTGRES_USER: ${{ secrets.POSTGRES_USER_TEST || 'postgres' }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST || 'postgres' }}
      POSTGRES_DB: finance_tracker_test
      # Test configuration
      NODE_ENV: test
      JWT_SECRET: ${{ secrets.JWT_SECRET_TEST || 'test_jwt_secret_for_ci_fallback' }}
      REFRESH_SECRET: ${{ secrets.REFRESH_SECRET_TEST || 'test_refresh_secret_for_ci_fallback' }}
      INTERNAL_SECRET: ${{ secrets.INTERNAL_SECRET_TEST || 'test_internal_secret_for_ci_fallback' }}
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD || '' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24.11.0'
      - name: Install dependencies
        run: |
          # Install dependencies including optional dependencies (SWC binaries)
          npm ci --include=optional
      - name: Install Next.js SWC binaries for Linux
        run: |
          # Next.js Jest transformer requires SWC binaries for Linux x64
          # Install them explicitly in the client workspace to ensure availability in CI
          cd apps/client
          echo "Installing Next.js SWC binaries for Linux x64..."
          # Install SWC packages - Next.js version must match the installed Next.js version
          npm install --save-optional --no-save @next/swc-linux-x64-gnu@^16.0.10 @next/swc-linux-x64-musl@^16.0.10
          echo "Verifying SWC binaries installation..."
          ls -la node_modules/@next/swc-linux-x64-* 2>/dev/null || echo "SWC packages location check"
      - name: Run client tests
        env:
          # NEXT_PUBLIC_API_BASE_URL is optional - tests mock API calls
          NEXT_PUBLIC_API_BASE_URL: 'http://localhost:8000'
          # INTERNAL_SECRET required for server-to-server authentication in API routes
          INTERNAL_SECRET: ${{ secrets.INTERNAL_SECRET_TEST || 'test_internal_secret_for_ci_fallback' }}
          # Help Next.js find SWC binaries
          NEXT_SKIP_SWC_BINARY_DOWNLOAD: 'false'
        run: |
          cd apps/client
          npm test -- --passWithNoTests --ci --coverage=false
      - name: Generate Prisma Client
        run: |
          cd apps/server
          export DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}"
          npx prisma generate
      - name: Build server
        run: |
          cd apps/server
          npm run build
      - name: Setup database
        run: |
          cd apps/server
          export DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}"
          npx prisma migrate deploy
          npx prisma db seed
      - name: Run server e2e tests
        run: |
          cd apps/server
          export DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}"
          npm test
